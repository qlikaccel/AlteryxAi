{
  "name": "Enterprise_Sales_Customer_Analytics_Pipeline",
  "compatibilityLevel": 1550,
  "model": {
    "culture": "en-US",
    "dataAccessOptions": {
      "legacyRedirects": true,
      "returnErrorValuesAsNull": true
    },
    "defaultPowerBIDataSourceVersion": "powerBI_V3",
    "sourceQueryCulture": "en-US",
    "tables": [
      {
        "name": "sales_transactions_2023_raw",
        "columns": [
          {
            "name": "TxnID",
            "dataType": "string",
            "sourceColumn": "TxnID",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "CustomerID",
            "dataType": "string",
            "sourceColumn": "CustomerID",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "ProductID",
            "dataType": "string",
            "sourceColumn": "ProductID",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "RegionCode",
            "dataType": "string",
            "sourceColumn": "RegionCode",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "SalesRep",
            "dataType": "string",
            "sourceColumn": "SalesRep",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "TxnDate",
            "dataType": "string",
            "sourceColumn": "TxnDate",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "Amount",
            "dataType": "string",
            "sourceColumn": "Amount",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "Quantity",
            "dataType": "string",
            "sourceColumn": "Quantity",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "Discount",
            "dataType": "string",
            "sourceColumn": "Discount",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "PaymentType",
            "dataType": "string",
            "sourceColumn": "PaymentType",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "Status",
            "dataType": "string",
            "sourceColumn": "Status",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "Notes",
            "dataType": "string",
            "sourceColumn": "Notes",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "RawPhone",
            "dataType": "string",
            "sourceColumn": "RawPhone",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "RawEmail",
            "dataType": "string",
            "sourceColumn": "RawEmail",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "Tags",
            "dataType": "string",
            "sourceColumn": "Tags",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          }
        ],
        "partitions": [
          {
            "name": "sales_transactions_2023_raw-Partition",
            "mode": "import",
            "source": {
              "type": "m",
              "expression": [
                "let",
                "    Source = SharePoint.Files(\"https://sorimtechnologies.sharepoint.com\", [ApiVersion = 15]),",
                "    MatchingFiles = Table.SelectRows(Source, each [Name] = \"sales_transactions_2023.csv\"),",
                "    FileContent = if Table.RowCount(MatchingFiles) = 0 then error \"File not found in SharePoint: sales_transactions_2023.csv\" else MatchingFiles{0}[Content],",
                "    CsvData = Csv.Document(FileContent, [Delimiter = \",\", Encoding = 65001, QuoteStyle = QuoteStyle.Csv]),",
                "    PromotedHeaders = Table.PromoteHeaders(CsvData, [PromoteAllScalars = true]),",
                "    ChangedTypes = Table.TransformColumnTypes(PromotedHeaders, {{\"TxnID\", type text}, {\"CustomerID\", type text}, {\"ProductID\", type text}, {\"RegionCode\", type text}, {\"SalesRep\", type text}, {\"TxnDate\", type text}, {\"Amount\", type number}, {\"Quantity\", Int64.Type}, {\"Discount\", type number}, {\"PaymentType\", type text}, {\"Status\", type text}, {\"Notes\", type text}, {\"RawPhone\", type text}, {\"RawEmail\", type text}, {\"Tags\", type text}})",
                "in",
                "    ChangedTypes"
              ]
            }
          }
        ]
      },
      {
        "name": "customer_master_500k_raw",
        "columns": [
          {
            "name": "CustomerID",
            "dataType": "string",
            "sourceColumn": "CustomerID",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "FullName",
            "dataType": "string",
            "sourceColumn": "FullName",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "Segment",
            "dataType": "string",
            "sourceColumn": "Segment",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "Country",
            "dataType": "string",
            "sourceColumn": "Country",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "City",
            "dataType": "string",
            "sourceColumn": "City",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "JoinDate",
            "dataType": "string",
            "sourceColumn": "JoinDate",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "Tier",
            "dataType": "string",
            "sourceColumn": "Tier",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "CreditLimit",
            "dataType": "string",
            "sourceColumn": "CreditLimit",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "AccountManager",
            "dataType": "string",
            "sourceColumn": "AccountManager",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "Phone",
            "dataType": "string",
            "sourceColumn": "Phone",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "Email",
            "dataType": "string",
            "sourceColumn": "Email",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "DOB",
            "dataType": "string",
            "sourceColumn": "DOB",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "IsActive",
            "dataType": "string",
            "sourceColumn": "IsActive",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          }
        ],
        "partitions": [
          {
            "name": "customer_master_500k_raw-Partition",
            "mode": "import",
            "source": {
              "type": "m",
              "expression": [
                "let",
                "    Source = SharePoint.Files(\"https://sorimtechnologies.sharepoint.com\", [ApiVersion = 15]),",
                "    MatchingFiles = Table.SelectRows(Source, each [Name] = \"customer_master_500k.csv\"),",
                "    FileContent = if Table.RowCount(MatchingFiles) = 0 then error \"File not found in SharePoint: customer_master_500k.csv\" else MatchingFiles{0}[Content],",
                "    CsvData = Csv.Document(FileContent, [Delimiter = \",\", Encoding = 65001, QuoteStyle = QuoteStyle.Csv]),",
                "    PromotedHeaders = Table.PromoteHeaders(CsvData, [PromoteAllScalars = true]),",
                "    ChangedTypes = Table.TransformColumnTypes(PromotedHeaders, {{\"CustomerID\", type text}, {\"FullName\", type text}, {\"Segment\", type text}, {\"Country\", type text}, {\"City\", type text}, {\"JoinDate\", type text}, {\"Tier\", type text}, {\"CreditLimit\", type number}, {\"AccountManager\", type text}, {\"Phone\", type text}, {\"Email\", type text}, {\"DOB\", type text}, {\"IsActive\", type text}})",
                "in",
                "    ChangedTypes"
              ]
            }
          }
        ]
      },
      {
        "name": "product_catalog_100k_raw",
        "columns": [
          {
            "name": "ProductID",
            "dataType": "string",
            "sourceColumn": "ProductID",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "ProductName",
            "dataType": "string",
            "sourceColumn": "ProductName",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "Category",
            "dataType": "string",
            "sourceColumn": "Category",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "SubCategory",
            "dataType": "string",
            "sourceColumn": "SubCategory",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "UnitCost",
            "dataType": "string",
            "sourceColumn": "UnitCost",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "ListPrice",
            "dataType": "string",
            "sourceColumn": "ListPrice",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "Currency",
            "dataType": "string",
            "sourceColumn": "Currency",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "LaunchDate",
            "dataType": "string",
            "sourceColumn": "LaunchDate",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "IsDiscontinued",
            "dataType": "string",
            "sourceColumn": "IsDiscontinued",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          }
        ],
        "partitions": [
          {
            "name": "product_catalog_100k_raw-Partition",
            "mode": "import",
            "source": {
              "type": "m",
              "expression": [
                "let",
                "    Source = SharePoint.Files(\"https://sorimtechnologies.sharepoint.com\", [ApiVersion = 15]),",
                "    MatchingFiles = Table.SelectRows(Source, each [Name] = \"product_catalog_100k.csv\"),",
                "    FileContent = if Table.RowCount(MatchingFiles) = 0 then error \"File not found in SharePoint: product_catalog_100k.csv\" else MatchingFiles{0}[Content],",
                "    CsvData = Csv.Document(FileContent, [Delimiter = \",\", Encoding = 65001, QuoteStyle = QuoteStyle.Csv]),",
                "    PromotedHeaders = Table.PromoteHeaders(CsvData, [PromoteAllScalars = true]),",
                "    ChangedTypes = Table.TransformColumnTypes(PromotedHeaders, {{\"ProductID\", type text}, {\"ProductName\", type text}, {\"Category\", type text}, {\"SubCategory\", type text}, {\"UnitCost\", type number}, {\"ListPrice\", type number}, {\"Currency\", type text}, {\"LaunchDate\", type text}, {\"IsDiscontinued\", type text}})",
                "in",
                "    ChangedTypes"
              ]
            }
          }
        ]
      },
      {
        "name": "Enterprise_Sales_Customer_Analytics_Pipeline",
        "columns": [
          {
            "name": "RegionCode",
            "dataType": "string",
            "sourceColumn": "RegionCode",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "Category",
            "dataType": "string",
            "sourceColumn": "Category",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "TxnQuarter",
            "dataType": "string",
            "sourceColumn": "TxnQuarter",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "TxnYear",
            "dataType": "string",
            "sourceColumn": "TxnYear",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "DealTier",
            "dataType": "string",
            "sourceColumn": "DealTier",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "PaymentGroup",
            "dataType": "string",
            "sourceColumn": "PaymentGroup",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "TotalNetRevenue",
            "dataType": "string",
            "sourceColumn": "TotalNetRevenue",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "TransactionCount",
            "dataType": "string",
            "sourceColumn": "TransactionCount",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "AvgTransactionValue",
            "dataType": "string",
            "sourceColumn": "AvgTransactionValue",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "MinTransaction",
            "dataType": "string",
            "sourceColumn": "MinTransaction",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "MaxTransaction",
            "dataType": "string",
            "sourceColumn": "MaxTransaction",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "TotalQuantity",
            "dataType": "string",
            "sourceColumn": "TotalQuantity",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "AvgDiscount",
            "dataType": "string",
            "sourceColumn": "AvgDiscount",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          },
          {
            "name": "UrgentCount",
            "dataType": "string",
            "sourceColumn": "UrgentCount",
            "summarizeBy": "none",
            "annotations": [
              {
                "name": "SummarizationSetBy",
                "value": "Automatic"
              }
            ]
          }
        ],
        "partitions": [
          {
            "name": "Enterprise_Sales_Customer_Analytics_Pipeline-Partition",
            "mode": "import",
            "source": {
              "type": "m",
              "expression": [
                "let",
                "    Source = SharePoint.Files(\"https://sorimtechnologies.sharepoint.com\", [ApiVersion = 15]),",
                "    MatchingFiles = Table.SelectRows(Source, each [Name] = \"sales_transactions_2023.csv\"),",
                "    FileContent = if Table.RowCount(MatchingFiles) = 0 then error \"File not found in SharePoint: sales_transactions_2023.csv\" else MatchingFiles{0}[Content],",
                "    CsvData = Csv.Document(FileContent, [Delimiter = \",\", Encoding = 65001, QuoteStyle = QuoteStyle.Csv]),",
                "    PromotedHeaders = Table.PromoteHeaders(CsvData, [PromoteAllScalars = true]),",
                "    ChangedTypes = Table.TransformColumnTypes(PromotedHeaders, {{\"TxnID\", type text}, {\"CustomerID\", type text}, {\"ProductID\", type text}, {\"RegionCode\", type text}, {\"SalesRep\", type text}, {\"TxnDate\", type text}, {\"Amount\", type number}, {\"Quantity\", Int64.Type}, {\"Discount\", type number}, {\"PaymentType\", type text}, {\"Status\", type text}, {\"Notes\", type text}, {\"RawPhone\", type text}, {\"RawEmail\", type text}, {\"Tags\", type text}}),",
                "    Checkpoint_1 = ChangedTypes,",
                "    ManualReview_2 = Checkpoint_1,",
                "    ManualReview_3 = ManualReview_2,",
                "    SplitColumnsPrepared_4 = ManualReview_3,",
                "    ManualReview_5 = SplitColumnsPrepared_4,",
                "    Calculated_RecencyBucket_6 = let Input = let required = {\"DaysSinceTxn\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"PaymentType\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\", \"Discount\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnQuarter\", each try (\"Q\" & Text.From(Number.RoundUp(Date.Month(DateTime.Date([TxnDate_Parsed])) / 3.0))) otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnQuarter\", each try (\"Q\" & Text.From(Number.RoundUp(Date.Month(DateTime.Date([TxnDate_Parsed])) / 3.0))) otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"NetAmount\", each try ([Amount] * (1 - [Discount])) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\", \"Discount\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnQuarter\", each try (\"Q\" & Text.From(Number.RoundUp(Date.Month(DateTime.Date([TxnDate_Parsed])) / 3.0))) otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnQuarter\", each try (\"Q\" & Text.From(Number.RoundUp(Date.Month(DateTime.Date([TxnDate_Parsed])) / 3.0))) otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"NetAmount\", each try ([Amount] * (1 - [Discount])) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"PaymentGroup\", each try (if Text.Contains(Text.From([PaymentType]), \"Card\") then \"Card\" else if Text.Contains(Text.From([PaymentType]), \"Transfer\") then \"Bank\" else if [PaymentType] = \"Cash\" then \"Cash\" else \"Other\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"PaymentType\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\", \"Discount\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnQuarter\", each try (\"Q\" & Text.From(Number.RoundUp(Date.Month(DateTime.Date([TxnDate_Parsed])) / 3.0))) otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnQuarter\", each try (\"Q\" & Text.From(Number.RoundUp(Date.Month(DateTime.Date([TxnDate_Parsed])) / 3.0))) otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"NetAmount\", each try ([Amount] * (1 - [Discount])) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\", \"Discount\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnQuarter\", each try (\"Q\" & Text.From(Number.RoundUp(Date.Month(DateTime.Date([TxnDate_Parsed])) / 3.0))) otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnQuarter\", each try (\"Q\" & Text.From(Number.RoundUp(Date.Month(DateTime.Date([TxnDate_Parsed])) / 3.0))) otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"NetAmount\", each try ([Amount] * (1 - [Discount])) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"PaymentGroup\", each try (if Text.Contains(Text.From([PaymentType]), \"Card\") then \"Card\" else if Text.Contains(Text.From([PaymentType]), \"Transfer\") then \"Bank\" else if [PaymentType] = \"Cash\" then \"Cash\" else \"Other\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DaysSinceTxn\", each try (Duration.Days(DateTime.Date(DateTime.LocalNow()) - DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"PaymentType\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\", \"Discount\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnQuarter\", each try (\"Q\" & Text.From(Number.RoundUp(Date.Month(DateTime.Date([TxnDate_Parsed])) / 3.0))) otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnQuarter\", each try (\"Q\" & Text.From(Number.RoundUp(Date.Month(DateTime.Date([TxnDate_Parsed])) / 3.0))) otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"NetAmount\", each try ([Amount] * (1 - [Discount])) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\", \"Discount\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnQuarter\", each try (\"Q\" & Text.From(Number.RoundUp(Date.Month(DateTime.Date([TxnDate_Parsed])) / 3.0))) otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnQuarter\", each try (\"Q\" & Text.From(Number.RoundUp(Date.Month(DateTime.Date([TxnDate_Parsed])) / 3.0))) otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"NetAmount\", each try ([Amount] * (1 - [Discount])) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"PaymentGroup\", each try (if Text.Contains(Text.From([PaymentType]), \"Card\") then \"Card\" else if Text.Contains(Text.From([PaymentType]), \"Transfer\") then \"Bank\" else if [PaymentType] = \"Cash\" then \"Cash\" else \"Other\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"PaymentType\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\", \"Discount\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnQuarter\", each try (\"Q\" & Text.From(Number.RoundUp(Date.Month(DateTime.Date([TxnDate_Parsed])) / 3.0))) otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnQuarter\", each try (\"Q\" & Text.From(Number.RoundUp(Date.Month(DateTime.Date([TxnDate_Parsed])) / 3.0))) otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"NetAmount\", each try ([Amount] * (1 - [Discount])) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\", \"Discount\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnQuarter\", each try (\"Q\" & Text.From(Number.RoundUp(Date.Month(DateTime.Date([TxnDate_Parsed])) / 3.0))) otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"TxnDate_Parsed\"}, existing = Table.ColumnNames(let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Notes\"}, existing = Table.ColumnNames(let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text)), missing = List.Difference(required, existing) in List.Accumulate(missing, let Input = let required = {\"Amount\"}, existing = Table.ColumnNames(ManualReview_5), missing = List.Difference(required, existing) in List.Accumulate(missing, ManualReview_5, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DealTier\", each try (if [Amount] >= 100000 then \"Enterprise\" else if [Amount] >= 25000 then \"Mid-Market\" else if [Amount] >= 5000 then \"SMB\" else \"Micro\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"IsUrgent\", each try (Text.Contains(Text.Lower(Text.From([Notes])), \"urgent\") or Text.Contains(Text.Lower(Text.From([Notes])), \"priority\")) otherwise null, type logical), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnYear\", each try (Date.Year(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnMonth\", each try (Date.Month(DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"TxnQuarter\", each try (\"Q\" & Text.From(Number.RoundUp(Date.Month(DateTime.Date([TxnDate_Parsed])) / 3.0))) otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"NetAmount\", each try ([Amount] * (1 - [Discount])) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"PaymentGroup\", each try (if Text.Contains(Text.From([PaymentType]), \"Card\") then \"Card\" else if Text.Contains(Text.From([PaymentType]), \"Transfer\") then \"Bank\" else if [PaymentType] = \"Cash\" then \"Cash\" else \"Other\") otherwise null, type text), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"DaysSinceTxn\", each try (Duration.Days(DateTime.Date(DateTime.LocalNow()) - DateTime.Date([TxnDate_Parsed]))) otherwise null, type number), (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.AddColumn(Input, \"RecencyBucket\", each try (if [DaysSinceTxn] <= 30 then \"Hot\" else if [DaysSinceTxn] <= 90 then \"Warm\" else if [DaysSinceTxn] <= 365 then \"Cold\" else \"Inactive\") otherwise null, type text),",
                "    Calculated_PaymentType_7 = let cols = Table.ColumnNames(Calculated_RecencyBucket_6), ops = {{\"RegionCode\", each Text.Trim(Text.Upper(Text.From(_))), type text}, {\"Status\", each Text.Trim(Text.Upper(Text.From(_))), type text}, {\"PaymentType\", each Text.Trim(Text.Upper(Text.From(_))), type text}} in Table.TransformColumns(Calculated_RecencyBucket_6, List.Select(ops, each List.Contains(cols, _{0}))),",
                "    ReplacePrepared_8 = Calculated_PaymentType_7,",
                "    FilteredRows_9 = let required = {\"Status\", \"IsValidEmail\", \"NetAmount\"}, existing = Table.ColumnNames(ReplacePrepared_8), missing = List.Difference(required, existing) in if List.Count(missing) > 0 then ReplacePrepared_8 else Table.SelectRows(ReplacePrepared_8, each try (List.Contains({\"COMPLETED\", \"IN_PROGRESS\"}, [Status]) and [IsValidEmail] = true and [NetAmount] > 0) otherwise false),",
                "    JoinPrepared_10 = FilteredRows_9,",
                "    JoinPrepared_11 = JoinPrepared_10,",
                "    AppendFieldsPrepared_12 = JoinPrepared_11,",
                "    JoinPrepared_13 = AppendFieldsPrepared_12,",
                "    SortedRows_14 = JoinPrepared_13,",
                "    Calculated_TxnSeq_Customer_15 = let Input = let Input = SortedRows_14 in Table.AddColumn(Input, \"RunningTotal_Customer\", each try (null) otherwise null, type number) in Table.AddColumn(Input, \"TxnSeq_Customer\", each try (null) otherwise null, type number),",
                "    GroupedData_16 = let Input = let required = {\"RegionCode\", \"Category\", \"TxnQuarter\", \"TxnYear\", \"DealTier\", \"PaymentGroup\", \"NetAmount\", \"Quantity\", \"Discount\", \"IsUrgent\"}, existing = Table.ColumnNames(Calculated_TxnSeq_Customer_15), missing = List.Difference(required, existing) in List.Accumulate(missing, Calculated_TxnSeq_Customer_15, (state, col) => Table.AddColumn(state, col, each null, type any)) in Table.Group(Input, {\"RegionCode\", \"Category\", \"TxnQuarter\", \"TxnYear\", \"DealTier\", \"PaymentGroup\"}, {{\"TotalNetRevenue\", each List.Sum([NetAmount]), type number}, {\"TransactionCount\", each Table.RowCount(_), Int64.Type}, {\"AvgTransactionValue\", each List.Average([NetAmount]), type number}, {\"MinTransaction\", each List.Min([NetAmount]), type any}, {\"MaxTransaction\", each List.Max([NetAmount]), type any}, {\"TotalQuantity\", each List.Sum([Quantity]), type number}, {\"AvgDiscount\", each List.Average([Discount]), type number}, {\"UrgentCount\", each Table.RowCount(_), Int64.Type}}),",
                "    PivotPrepared_17 = GroupedData_16,",
                "    UnionPrepared_18 = PivotPrepared_17,",
                "    Checkpoint_19 = UnionPrepared_18",
                "in",
                "    Checkpoint_19"
              ]
            }
          }
        ]
      }
    ],
    "relationships": [],
    "expressions": [
      {
        "name": "DataSourcePath",
        "kind": "m",
        "expression": [
          "\"\""
        ],
        "annotations": [
          {
            "name": "IsParameterQuery",
            "value": "True"
          },
          {
            "name": "IsParameterQueryRequired",
            "value": "False"
          },
          {
            "name": "PBI_QueryOrder",
            "value": "0"
          }
        ]
      }
    ],
    "annotations": [
      {
        "name": "PBIDesktopVersion",
        "value": "2.130.930.0"
      },
      {
        "name": "createdBy",
        "value": "QlikAI_Accelerator"
      }
    ]
  }
}